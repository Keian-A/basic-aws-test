name: Deploy The Server
on:
  push:
    branches:
    - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

    - name: Checkout source code
      uses: actions/checkout@v1

    - name: Generate deployment package
      run: zip -r deploy.zip . -x '*.git*'

    - name: Deploy to EB
      uses: einaregilsson/beanstalk-deploy@v16
      with:
        aws_access_key: ${{ AKIARHSAY7XSSDELWDGY }}
        aws_secret_key: ${{ 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 }}
        application_name: keian-aws-test
        environment_name: basic-aws-test
        version_label: 12345
        region: us-west-2
        deployment_package: deploy.zip